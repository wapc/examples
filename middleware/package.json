{
  "name": "middleware",
  "version": "0.0.1",
  "description": "HTTP middleware",
  "author": "waPC authors",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "asc assembly/index.ts -o build/middleware-as.wasm --use abort=assembly/index/abort --optimize"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@wapc/as-guest": "^v0.3.1",
    "@wapc/as-msgpack": "^0.1.15"
  },
  "devDependencies": {
    "assemblyscript": "^0.21.3"
  }
}
